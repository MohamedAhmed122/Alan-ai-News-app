const { makeStyles } = require("@material-ui/core");

export default makeStyles((theme) => ({
  appBar: {
    backgroundColor: "#1976d2",
    alignItems: "center",
    color: "white",
  },
  ToolbarMargin: {
    marginBottom: "2em",
  },
}));
